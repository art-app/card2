FROM nginx:alpine

# Статичная страница Mini App — единственный файл.
COPY index.html /usr/share/nginx/html/index.html

# nginx:alpine по умолчанию слушает 80 и отдаёт /usr/share/nginx/html
EXPOSE 80
