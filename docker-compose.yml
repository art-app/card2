services:
  webapp:
    # На сервере образ берётся из GHCR (docker compose pull).
    # build остаётся для локальной сборки (docker compose build).
    image: ${WEBAPP_IMAGE:-ghcr.io/art-app/trainer-webapp}:${IMAGE_TAG:-latest}
    build: ./webapp
    container_name: trainer-webapp
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=${TRAEFIK_NETWORK}"
      # HTTPS-роутер
      - "traefik.http.routers.trainer.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.trainer.entrypoints=${TRAEFIK_ENTRYPOINT}"
      - "traefik.http.routers.trainer.tls=true"
      - "traefik.http.routers.trainer.tls.certresolver=${TRAEFIK_CERTRESOLVER}"
      - "traefik.http.services.trainer.loadbalancer.server.port=80"

  bot:
    image: ${BOT_IMAGE:-ghcr.io/art-app/trainer-bot}:${IMAGE_TAG:-latest}
    build: ./bot
    container_name: trainer-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEBAPP_URL=https://${DOMAIN}/
    depends_on:
      - webapp

networks:
  # Внешняя сеть, которую слушает Traefik (создаётся не этим compose).
  proxy:
    external: true
    name: ${TRAEFIK_NETWORK}
