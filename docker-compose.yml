services:
  webapp:
    # На сервере образ берётся из GHCR (docker compose pull).
    # build остаётся для локальной сборки (docker compose build).
    image: ${WEBAPP_IMAGE:-ghcr.io/art-app/card2-webapp}:${IMAGE_TAG:-latest}
    build: ./webapp
    container_name: trener-webapp
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=${TRAEFIK_NETWORK}"
      # HTTPS-роутер
      - "traefik.http.routers.trener.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.trener.entrypoints=${TRAEFIK_ENTRYPOINT}"
      - "traefik.http.routers.trener.tls=true"
      - "traefik.http.routers.trener.tls.certresolver=${TRAEFIK_CERTRESOLVER}"
      - "traefik.http.services.trener.loadbalancer.server.port=80"

  bot:
    image: ${BOT_IMAGE:-ghcr.io/art-app/card2-bot}:${IMAGE_TAG:-latest}
    build: ./bot
    container_name: trener-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEBAPP_URL=https://${DOMAIN}/
    depends_on:
      - webapp

networks:
  # Внешняя сеть, которую слушает Traefik (создаётся не этим compose).
  proxy:
    external: true
    name: ${TRAEFIK_NETWORK}
